# 电商智能客服工单系统 用户手册

## 系统简介

本系统是一套基于 FastAPI、LangGraph、RAG、MCP 和 Vue 3 的电商客服工单系统，覆盖工单全流程、AI 自动处理和人工审核，并提供监控、日志和反馈闭环能力。

## 快速启动

环境要求：Docker Desktop 已启动，并且 `docker compose` 可用。

1. 进入项目目录：`cd ecommerce-cs-agent`
2. 配置环境变量：`cp .env.example .env`，填入 `OPENAI_API_KEY`
3. 构建并启动：`docker compose up -d --build`
4. 查看启动状态：`docker compose ps`
5. 停止服务：`docker compose down`

## 服务地址

| 服务 | 地址 | 说明 |
|------|------|------|
| Web 前端 | http://localhost:5173 | 客服工单管理系统 |
| API 文档 | http://localhost:8000/docs | Swagger 接口文档 |
| Prometheus | http://localhost:9090 | 指标查询与告警规则 |
| Grafana | http://localhost:3000 | 监控仪表盘 |
| Alertmanager | http://localhost:9093 | 告警管理 |
| Redis Exporter | http://localhost:9121 | Redis 指标 |

Grafana 默认管理员账号为 `admin / admin`，可通过 `.env` 中的 `GRAFANA_ADMIN_USER` 和 `GRAFANA_ADMIN_PASSWORD` 修改。

## 演示账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 客户 | customer | customer123 |
| 客服 | staff | staff123 |
| 客服主管 | supervisor | supervisor123 |
| 管理员 | admin | admin123 |

## 工单使用

- 客户登录后可创建工单、上传附件、查看处理进度并评价满意度。
- 客服登录后可查看待处理工单、阅读 AI 回复草稿、修改后发送，并查看 AI 推荐的知识库内容。
- 客服主管或管理员可在「客服工作台」查看待分配、待人工审核和全部进行中工单，领取工单或指派给其他客服，普通客服只能领取自己的工单。
- 管理员可查看统计看板、AI 效果评估、知识库和 RLHF 数据。

## 知识库

知识库页面支持新增、编辑、删除 FAQ 文档。系统启动时会自动重建向量索引，RAG 检索会同时使用 ChromaDB 向量检索和 TF-IDF 关键词检索，再以 RRF 方式融合排序。

## 满意度与 RLHF

- 工单处理完成后，客户可以按 1-5 分评价。
- RLHF 页面支持记录 AI 回复、人工修正、标签和评分，并可导出 `chosen/rejected` 偏好数据集，用于后续模型微调。

## MCP 接入

启动 MCP Server：

```bash
cd ecommerce-cs-agent/backend
python -m app.mcp_server
```

Claude Desktop / Cursor MCP 配置示例：

```json
{
  "mcpServers": {
    "ecommerce-cs-agent": {
      "command": "python",
      "args": ["C:/Users/熊汉/Desktop/codex/ecommerce-cs-agent/backend/app/mcp_server.py"]
    }
  }
}
```

可用工具包括工单 CRUD、RAG 搜索、知识库管理、反馈记录、RLHF 导出、通知和邮件渠道。

## 监控与告警

- Prometheus 每 15 秒抓取 `backend:8000/api/metrics` 和 Redis Exporter 指标。
- Grafana 已预置“电商客服工单系统”仪表盘，展示工单数、已解决数、RLHF 数据量、LLM 延迟和服务可用性。
- Prometheus 内置告警规则：后端不可用、Redis 不可用、LLM 平均延迟超过 2000ms。
- Alertmanager 统一接收告警，默认路由为本地静默接收，生产环境可补充企业微信、邮件或 Webhook 接收器。

## 日志

- 后端以 JSON 结构化日志输出到标准输出，可执行 `docker compose logs -f backend` 查看。
- Docker 日志按 10MB 单文件、最多保留 3 个文件自动轮转。
- Agent 执行日志按工单写入 `agent_logs` 数据表，可通过统计接口或数据库查询。
- 业务数据保存在 `ticket_data` 卷中，`docker compose down` 不会删除数据。

## 常见问题

1. `docker` 命令找不到：Windows 下可使用 Docker Desktop 安装目录中的 `resources\bin\docker.exe`，或重新登录终端刷新 PATH。
2. 端口被占用：修改 `docker-compose.yml` 中对应服务的 `ports` 映射。
3. AI 回复为空：检查 `.env` 中的 `OPENAI_API_KEY`、`OPENAI_BASE_URL` 和 `LLM_MODEL`。
4. 数据未生效：确认 `.env` 已配置，并重启容器 `docker compose up -d`。
