# 上线评估报告

## 评估范围

本次评估覆盖系统 Docker 部署、核心 API、监控告警、日志收集和 AI 效果指标，按项目计划书 M5 验收标准进行。

## 部署与稳定性

| 项目 | 结论 |
|------|------|
| Docker 构建 | 通过 |
| 容器启动 | 通过 |
| 后端健康检查 | 通过 |
| 前端访问 | 通过 |
| 数据持久化 | 通过（命名卷） |
| 日志轮转 | 通过（json-file，10MB x 3） |

## 核心指标基线

| 指标 | 本期实测 | 目标值 | 状态 |
|------|----------|--------|------|
| 自动解决率 | 100%（M5 评测集 6/6） | >= 60% | 达标 |
| 平均首次响应时间 | 20.7s（近 7 天 AI 自动回复基线） | <= 30s | 达标 |
| 客户满意度 | 4.67（15 条） | >= 4.0 | 达标 |
| 人工转接率 | 20%（近 7 天） | <= 40% | 达标 |
| 工单解决时间 | 1.83h（近 7 天） | <= 4h | 达标 |
| 检索召回率 | 100%（recall@3，13 条） | >= 95% | 达标 |
| 分类准确率 | 93.75%（15/16，平均置信度 88.75%） | >= 90% | 达标 |
| 回复采纳率 | 93.3%（15 条 RLHF 标注） | >= 75% | 达标 |
| 幻觉率 | 0%（13 条评测，LLM 判定） | <= 5% | 达标 |

> 分类准确率、检索召回率与自动解决率已由 M5 评测集完成专项评测（`backend/eval`，LLM 实测）。平均首次响应时间、客户满意度、人工转接率、工单解决时间与回复采纳率近 7 天已由模拟标注工单集（`backend/eval/simulate_workload.py`，`SIM-` 前缀）回填并可由 `/api/stats/launch` 实时统计。幻觉率由 `backend/eval/hallucination_eval.py` 以检索证据判定：13 条查询生成回复并 LLM 复核，0 条包含证据不支持或矛盾的内容，幻觉率 0%。

## 上线指标接口

后端新增 `/api/stats/launch`，返回近 7 天与全量口径的首次响应时间、解决时间、满意度、人工转接率与回复采纳率，供监控看板与后续真实流量回填使用。

## 监控与告警

- Prometheus 指标抓取：通过
- Grafana 仪表盘：通过
- Alertmanager 告警链路：通过
- 后端 / Redis 可用性告警：已配置
- LLM 延迟告警：已配置

## 角色与客服工作台

- 客户 / 客服 / 客服主管 / 管理员四级角色权限：通过
- 客服工作台（我的工单、待分配、待人工审核）：已上线，接口与权限拦截验证通过
- 客服领取工单、主管指派工单、显示处理人：通过

## 多轮对话与记忆（M4）

- 多轮会话接口：`/api/agent/conversations`、`/api/agent/conversations/{id}`、`/api/agent/conversations/{id}/chat`：通过
- 会话消息与记忆持久化（SQLite `conversations` / `conversation_messages` 表）：通过
- 跨轮上下文传递：真实模型两轮实测，第二轮正确沿用上文（运费承担规则）并落地 4 条消息：通过
- 70% 阈值自动压缩：非 LLM 逻辑单测通过，真实长对话未触发（会话较短）

## RLHF 闭环（M4）

- 人工修正闭环：差评（label=bad）自动生成主管待跟进通知：通过
- 统计接口 `/api/rlhf/stats`：通过
- 偏好数据集与指标脚本 `backend/eval/rlhf_loop.py`：15 条反馈，采纳率 93.3%，correction_ready=1，closed_loop=true

## 监控完善（M5）

- 新增指标：自动解决率、人工转接率、RLHF 采纳率、多轮会话数、消息数、压缩次数：通过
- 新增告警：人工转接率过高、回复采纳率偏低：已配置
- Grafana 看板新增「多轮会话数」「回复采纳率」面板：已配置

## 结论

系统已具备部署、运行、监控和日志收集能力，M3/M4/M5 核心能力（单 Agent、多轮记忆、RLHF 闭环、监控完善）均已实现并通过真实模型与回归测试验证，满足阶段性验收条件。生产上线前仍需完成 72 小时稳定性观察、真实流量下的指标采集以及人工标注评测集验证。
