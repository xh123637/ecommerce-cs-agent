# 《面试防拷打笔记》

> 规则：每一条用【问题/根因/解决/知识点/面试官追问】五段式记录；按模块归类；只追加不覆盖。

## A. 框架选型

### A-1 为什么 LangGraph 而不是手写状态机

- 【问题】自动处理工单需要稳定走“分类 → RAG 检索 → 生成回复 → 监督转人工”，又允许 Agent 自己选工具，如何避免混乱的 if/else。
- 【根因】客服工单处理本质是有阶段、阶段内又有决策分支的工作流；纯代码状态机能跑但扩展慢，单次 LLM 编排全部步骤又不可控。LangGraph 把“有向图 + 状态 + 节点函数”做成显式编排。
- 【解决】`agent_graph.py` 里保留了 `classify -> search -> reply -> supervise` 的 LangGraph 图；`tool_agent.py` 再让单 Agent 自主调工具，LLM 不可用时回落到底层图。
- 【知识点】LangGraph 核心是 StateGraph、Node、Edge、State；Node 修改 state dict，Edge 可普通可条件。
- 【深度】中
- 【面试官追问】LangGraph state 在节点间怎么合并？并行节点如何汇总？答：可使用 reducer；并行节点多个输出聚合到同一个 state。

### A-2 单 Agent 多工具 vs 多 Agent 编排

- 【问题】要不要拆“分类 Agent / 检索 Agent / 回复 Agent”。
- 【根因】多 Agent 的收益是解耦与并行，代价是 context 串联复杂、调试慢、token 贵。本项目链路虽多步但是每步逻辑和工具有限，单 Agent 多工具已够。
- 【解决】当前采用单 Agent + 多工具调用（LangChain StructuredTool 包 MCP Server 的函数），用工具权限 tier 控制角色。
- 【知识点】工具编排就是 LLM 看到 schema、生成 function call、执行工具、回填 ToolMessage 再继续。
- 【面试官追问】什么时候必须拆多 Agent？隔离上下文/不同安全边界/不同模型/并行处理独立任务/单个工具列表太长时。

### A-3 为什么引入官方 MCP SDK

- 【问题】后端自己能直接调内部函数，为什么要再暴露成 MCP Server。
- 【根因】MCP 的价值是让外部 Agent 宿主（Claude Desktop / Cursor）用标准协议发现和调用工单能力，不重复适配。
- 【解决】用官方 `mcp.server.mcpserver.MCPServer` 暴露 `list_tickets / get_ticket / process_ticket / query_express` 等工具，再用 LangChain StructuredTool 包装成 Agent 工具。
- 【知识点】MCP 传输有 stdio 与 HTTP/SSE；本项目的 stdio 适合本地 Claude Desktop / Cursor。
- 【深度】中
- 【面试官追问】MCP Tool 和 Resource 区别？Tool 是动作，Resource 是可寻址数据，如 `ticket://tickets/{id}`。

## B. 状态图与条件路由

### B-1 低置信度转人工

- 【问题】LLM 可能给出不可信回复，客服场景不能直接放给客户。
- 【根因】分类置信度过低，或 LLM 自己标记 needs_human=true，结果不可靠。
- 【解决】监督节点根据 `confidence < CONFIDENCE_THRESHOLD` 或 `needs_human` 设状态为“待人工审核”，前端工作台有“待人工审核”队列，人工可改状态和解决方案。
- 【知识点】这是 human-in-the-loop 落点：机器给建议，人审核后采用。LangGraph 以后可用 checkpointer 暂停等待人工输入。
- 【面试官追问】人工修正如何回流？有 RLHF 表和主管通知；下一步可回灌知识库。

### B-2 工单对话角色设计

- 【问题】处理人在对话里输入“你好”，界面却显示“客服助手”AI 回复，处理人原文被丢。
- 【根因】旧实现把处理人发送也交给 LLM，处理人输入被当成给 AI 的指令。
- 【解决】改造 `ticket_conversation_chat`：客户发存 `user` 再调 AI 生成 `assistant`；处理人发直接存为 `human`，不调 AI 也不丢原文；前端按角色显示。
- 【知识点】工单会话是“接待上下文”，角色要区分 `user / assistant / human`，不只为模型服务，也要给客户看。
- 【面试官追问】客服如果先让 AI 起草再自己改呢？可以加 draft 草稿态：AI 生成到输入框，最终发送落 `human`。

## C. RAG

### C-1 ChromaDB 持久化、Embedding 与 RAG 搜索

- 【问题】RAG 需要把工单/知识库做向量搜索，进程重启后向量不能丢。
- 【根因】ChromaDB 可持久化到磁盘，需要持久目录；每次重建会浪费成本并丢历史。
- 【解决】项目配置 CHROMA_DIR，启动时 reindex_rag；搜索用向量 + TF-IDF + RRF，再做启发式重排。
- 【知识点】RAG 链路：切分/索引 -> 检索 -> 重排 -> 拼 prompt -> 生成；面试要区分召回与排序质量。
- 【深度】中
- 【面试追问】Embedding 选型？中文、成本、维度、是否和生成模型同厂商；本项目用本地 n-gram 哈希向量兜底。

### C-2 混合检索：向量 + 关键词 + RRF 融合

- 【问题】单向量检索经常“语义相似但缺专有词”，单关键词检索又容易被退货/退款/物流这类常见词带偏。
- 【根因】向量擅长语义近似，关键词擅长精确词面覆盖，两类排序分不可直接比。
- 【解决】`rag.py` 双路召回：
  - 向量路：ChromaDB 用 `HashingVectorizer` char n-gram 做 embedding，cosine 打分；
  - 关键词路：`TfidfVectorizer` char n-gram + `cosine_similarity` 打分，取更大候选；
  - 融合：RRF 按 `1/(60 + rank)` 累加，两边都命中的文档排名靠前。
- 【知识点】混合检索（Hybrid Search）+ RRF 是召回层最简单有效的套路；RRF 属于 rank-based fusion，不需要拖离两路分数。
- 【深度】中
- 【面试追问】为什么用 char n-gram 而不是中文分词？分词依赖词表，新词/错别字容易失配；char n-gram 对中文商品名和表达更稳定。

### C-3 重排序：两层排序体系

- 【问题】RRF 只保证“哪个文档被多个通道命中”，不代表“和 query 最像”。
- 【怎么做】`rag.py` 的 `_rerank_heuristic` 对 vector + keyword fusion 结果再做一轮 char n-gram Jaccard：
  `final_score = score * 0.6 + ngram_overlap * 0.4`。
- 【知识点】召回决定“候选池”，重排决定“最终排序”；重排可较贵，但只对召回 topN 做。
- 【深度】中
- 【面试追问】更好重排是什么？“cross-encoder / bge-reranker” 直接给 (query, doc) 打分，效果强但更慢，适合小召回集重排。

### C-4 如何提高召回率

- 【现状】当前 `vector_search(query, category, top_k)` 已支持分类过滤和启发式重排；`related_tickets` 会 `rerank=True`。想再提升召回，通常是这几点：
  - 查询扩展：把用户的话术扩展成“问题 + 分类 + 同义词”（未收到 → 未收到/丢件/在途）；
  - 多路召回：向量 + TF-IDF + 分类内检索，先取大候选再 RRF；
  - 先大后减：候选先 top 10-30，重排序后再压到 top 3；
  - 分类约束：在 where 里加 category，避免无关领域霸榜。
- 【知识点】Recall@K 是召回度量，不是感觉好就好；要建 golden（dev query -> 正确答案文档 id）后评估。
- 【深度】深
- 【面试追问】RAG 提升怎么证明？建 30-50 条人工评估集，跑旧/新检索，记录命中 doc 是否在 top K；否则口说“效果好”。

## 幻觉怎么降低

### H-1（RAG 视角）减少幻觉的三道防线

- 【问题】LLM 会一本正经地编造物流状态、退款原因、补偿金额。
- 【解法】不是只靠提示词，而是一套组合：
  1. 先检索/先调工具，把真实证据放进上下文；
  2. 有真实工具结果（如快递100）就按工具返回展示，不靠模型背；
  3. 置信度过低或召回空：设成“待人工审核”，不让模型直接硬答。
- 【证据】`express_service.query_express` 返回真实 `Traces`；`agent_graph._supervise_node` 按 `confidence < CONFIDENCE_THRESHOLD` 转人工。
- 【知识点】防幻觉 = 检索 + 工具 + 信任阈值 + 人工兜底；RAG 不是万能牌。
- 【面试追问】如果检索到错误资料但置信度很高怎么办？需要来源过滤、知识库版本权重、人工审核回流，和“回复尽量引用来源”的话术。

### H-2 本会话里真实遇到过的幻觉：物流工单被分类成“其他”

- 【现象】工单描述是“物流三天没更新 / 快递在转运中心卡住”，但 `agent_graph` 给它的分类是“其他”，RAG 按 `category=其他` 过滤，命中不到物流知识库，最后生成一条“已帮您记录，请留意后续通知”的泛泛回答。
- 【根因】两处叠加：
  1. 分类模型的 class 白名单里没有“物流”，模型没法合法输出 `物流`；
  2. 即使模型内心知道是物流问题，服务端也只认白名单，否则回退成 `其他`，导致检索和话术都跑偏。
    这看起来像“分类不准”，本质上是一种约束幻觉：LLM 能力到了，但业务约束没给它可选项。
- 【解决】把 `物流` 加入 `agent_graph` 合法分类集合；同时给工单加 `shipper_code/tracking_no` 结构化字段，`create_ticket` 在“有快递字段或描述含物流关键词”时自动归为物流，尽量不让分类模型独自背锅。
- 【结果】物流工单能正确进物流分类，详情页显示“物流轨迹”卡片，RAG 也按物流知识库召回，回复从“泛泛记录”变成“查真实轨迹 + 按超时/派送/签收处理”。
- 【可复用经验】别只依赖 LLM 分类；先用明确触发字段/规则把强信号接住，再用 LLM 处理模糊分类；同时 prompt 白名单和代码白名单必须保持一致，否则会静默回退。
- 【面试追问】如果 LLM 输出“物流”但用户描述里也有“退款”，怎么处理？可以多分类/置信度排序或“主分类 + 次分类”，并让 RAG 可跨两个 category 召回。

## D. MCP

### D-1 MCP 工具权限矩阵

- 【问题】MCP 工具直接暴露，客户也可能看到更新/删除工具，需要防越权。
- 【根因】MCP 只声明工具能力，不关心角色，应用层仍要鉴权。
- 【解决】`TOOL_TIERS` 定义 0=customer / 1=staff / 2=supervisor / 3=admin，`tools_for_role` 返回允许工具。
- 【知识点】MCP Server 还是“工具工厂”，业务权限必须应用层控制。
- 【深度】中
- 【面试追问】HTTP/SSE 传输怎么做鉴权？传输层包 Token，Server 端再按角色过滤工具。

## E. 数据存储

### E-1 SQLite 字段演进

- 【问题】工单要加“快递公司/运单号”，不想重建库。
- 【根因】SQLite 是零配置文件型，最适合开发；生产高并发换 Postgres。
- 【解决】init_db 用 `ALTER TABLE tickets ADD COLUMN shipper_code/tracking_no` 做轻量迁移。
- 【知识点】SQLite ALTER TABLE 支持有限；生产常见用 Alembic 等迁移工具。
- 【面试追问】SQLite 并发上限？写全局锁，同刻一个写事务；高写并发必须换 Postgres/云。

### E-2 时间存储与 UTC/本地时区

- 【问题】通知和消息显示时间比真实时间晚。
- 【根因】Docker 容器默认 UTC，后端 `datetime.now()` 生成 UTC，用户是 UTC+8。
- 【解决】`now_text()` 强制 `Asia/Shanghai`；历史数据一次性 +8 迁移。
- 【知识点】后端时间规范通常是存 UTC，前端按用户时区显示；已落库时间需要迁移。
- 【面试追问】生产系统应存 UTC 还是本地？应存 UTC，出参 ISO8601 带时区；本项目为了简单改成东八区。

## F. 前端

### F-1 Vue3 + Element Plus 前后端分离

- 【问题】Jinja2 服务端渲染对长异步 Agent 接口不友好，需要 SPA。
- 【根因】Agent 接口可能很慢（LLM），需要前端本地 loading、错误态。
- 【解决】Vue3 + Vite + Element Plus，JWT 登录，按角色显示；`client.post('/agent/...')` 调 FastAPI。
- 【知识点】前端隐藏不等于安全，真正权限在后端和工具 tier。
- 【面试追问】WebSocket 适配做实时工单状态？可以站内通知 + WS 推送；当前是重载/轮询。

## G. 工程化

### G-1 Docker Compose 环境变量透传

- 【问题】接入第三方接口，后端容器读不到 .env。
- 【根因】compose 不显式引用 `${VAR}`，根 .env 不会注入容器。
- 【解决】在 backend environment 加 `KUAI100_KEY: ${KUAI100_KEY:-}`；前端由 nginx 提供服务。
- 【知识点】Docker Compose 默认读项目根 .env，但只注入显式写在 compose 里的 VAR。
- 【面试追问】镜像构与上线迁移怎么保留数据？给 SQLite/Chroma 挂持久卷。

## H. 业务与权衡

### H-1 第三方物流接口接入

- 【问题】快递接口显示签名失败/查不到轨迹。
- 【根因】签名顺序错误；工单没有字段；描述无法识别快递单号。
- 【解决】先加工单 `shipper_code/tracking_no`，查询优先读工单字段，再回退描述识别；快递100签名 `MD5(param+key+customer)`，重 form POST。
- 【知识点】第三方物流接入常踩：表单 POST、签名顺序、URL 编码、真实单号和快递公司代码对齐。
- 【面试追问】三方超时失败怎么降级？缓存/重试/提示客服人工到官方查询。

### H-2 自动物流分类

- 【问题】新建工单不会自动进物流分类。
- 【解决】create_ticket 时：有快递字段直接归类物流；描述含“物流/快递/包裹/运单号/驿站/签收/派送/揽收/运输中/转运/快件”也归物流。
- 【知识点】规则分类稳定、可解释；AI 分类则依赖模型正确性，二者要结合。

## I. 开发过程问题发现与解决总结

> 本节记录我们在本会话中实际提出并修掉的问题；每条都按【现象/原因/修复/可复用经验】记录，适合复盘和面试讲“踩坑经历”。

### I-1 客服在工单对话发送消息，界面却显示 AI 回复

- 【现象】处理人登录后发送“你好”，对话里出现“客服助手”回复，处理人自己输入的内容没有出现在消息里。
- 【原因】旧逻辑把处理人发送也交给 LLM，把客服输入当作给 AI 的指令，而不是给客户的原文。
- 【修复】后端按角色分流：客户发送存 `user` 并调 Agent；处理人发送直接存 `human`，前端客户看到“客服”，处理人看到“我”。
- 【可复用点】chat/agent 系统的消息角色不只服务模型，也服务展示与权限；角色模型要同时考虑 `user/assistant/human`。

### I-2 通知文案对客户和客服/管理都长得一样

- 【现象】客户点开通知，标题还是内部工单语义（如“工单状态更新 | TK-016”），和客服/管理员收到的一样。
- 【原因】没有按受众做客户门户通知文案；数据其实已分受众，但文案仍偏内部。
- 【修复】后端对客户通知使用客户视角文案（如“您的工单有更新 / 状态已更新为「已解决」”）；前端把客户通知页标题改为“我的通知”，并区分空态。
- 【可复用点】通知系统应区分“内部通知”和“客户通知”，不能仅靠字体/颜色区分，数据模型和文案都要分角色。

### I-3 快递查询接入先报“非法参数”，又报“验证签名失败”

- 【现象】接快递鸟/快递100后，前端只有“暂无轨迹”，后端分别返回“快递鸟非法参数”和“快递100 验证签名失败”。
- 【原因】第一轮错误是快递鸟签名/编码不兼容；第二轮是快递100签名顺序为 `MD5(param+key+customer)`，不是 `MD5(param+customer+key)`；同时报错没有透传到前端，被“暂无轨迹”吞掉。
- 【修复】换快递100接口前定义标准 `KUAI100_KEY/CUSTOMER`，按文档修正签名顺序；后端对非 `200` 响应补 `error`/`message` 字段；前端保留接口真实信息，不只用“暂无轨迹”简化。
- 【验证/复现】TK-019 用旧假单号在快递100仍返回轨迹，说明链路已打通；最终问题定位是第三方凭据/签名/接口字段，而不是前端逻辑。
- 【可复用点】接外部服务时：先看原始响应，再判断是签名/请求/凭据问题；前端不要把第三方错误全部简化成“暂无数据”，否则会掩盖真实原因。

### I-4 Docker Compose 读不到 .env 的第三方 key

- 【现象】`.env` 中已填快递 key，但后端容器打日志还是“快递100未配置”。
- 【原因】Docker Compose 只向容器注入“写在 compose 文件里的 `${VAR}`”；已经配在后端 `.env` 里不会自动进容器。
- 【修复】在 `docker-compose.yml` backend environment 中显式加：
  - `KUAI100_KEY: ${KUAI100_KEY:-}`
  - `KUAI100_CUSTOMER: ${KUAI100_CUSTOMER:-}`
- 【可复用点】任何第三方密钥都写项目根 `.env` + compose 显式透传；否则本地能读、容器里读不到。

### I-5 时间显示不对，通知/消息比现实时间慢 8 小时

- 【现象】打开通知或聊天，时间明显和现实时间不一致，像“昨天”。
- 【原因】Docker 容器运行在 UTC，后端构造时间用了 `datetime.now()`，生成的是 UTC；项目是中国业务，不转 UTC+8 就错位。
- 【修复】`now_text()` 指定 `Asia/Shanghai`；已存在的历史时间做一次性 `+8 hours` 数据迁移。
- 【可复用点】容器时区不保证业务时区；时间字段要统一时区策略（建议存 UTC，前端按用户时区显示；至少生产系统明确时区再落库）。

### I-6 已有数据库不会获得新知识库/字段

- 【现象】新增 KB / 章节，已有 SQLite 库不会自动追加；加字段如果直接改建表，会破旧环境。
- 【原因】`seed_if_empty` 只在表空时初始化；`CREATE TABLE IF NOT EXISTS` 不能加新列。
- 【修复】知识库初始化和“补齐”都改为幂等 `INSERT OR IGNORE`；字段演进用 `ALTER TABLE ... ADD COLUMN` 兼容老库。
- 【可复用点】演示环境/线上迁移都要考虑“已有数据 + 新 schema”：种子数据可幂等追加，结构变更用迁移脚本。

### I-7 物流类工单没有结构化快递字段，导致查询不可靠

- 【现象】“查询物流”常常返回“未能识别到快递公司和运单号”或“暂无轨迹”。
- 【原因】工单表没有 `shipper_code/tracking_no`，所有信息靠描述里自然语言识别，识别率很低。
- 【修复】新增 `shipper_code` / `tracking_no` 字段；新建工单选“物流”分类后显示对应字段，后端也会优先读取字段；自动识别作为回退。
- 【可复用点】结构化强字段（如单号、公司码、手机号）不要依赖自然语言解析；建模时宁可先保留“不够熟”的字段，再配合解析回退。

### I-8 后端功能已上线，但用户看不到“查询物流”

- 【现象】我们已经把前端“物流轨迹 / 查询物流”打包并部署，用户在浏览器里明明打开工单却看不到。
- 【原因】浏览器缓存了旧 index/JS chunk，或者用户没有重新打开工单详情页，停留在旧页面状态；这不是后端没有，而是“看起来没有”。
- 【修复】强刷新（`Ctrl+F5`），并确认 nginx 返回的新 static chunk；必要时给静态资源加 hash（Vite 默认会带 hash），并以 `/assets/index-xxx.js` 是否变化作为判断依据。
- 【可复用点】联调时如果确认后端/前端服务已更新，先检查浏览器缓存和页面刷新；面试提“业务 bug 排查”时可以说“我先把问题分层：前端是否加载新 bundle -> 接口是否返回 -> 权限是否拦截”。

### I-9 第三方接口报错被前端统一显示成“暂无轨迹”

- 【现象】后端快递接口返回“快递鸟非法参数”或“快递100 验证签名失败”，但页面只显示“暂无轨迹”，导致像业务问题而不像接口问题。
- 【原因】前端判断只看了 `logistics` 有没有 Traces；第三方返回的 `message / reason / status` 没有透传到页面。
- 【修复】后端把快递服务非成功结果统一附上 `error`/`message`；前端先展示 `error`，没有轨迹但接口有 message 时展示真实 message，而不是直接写死“暂无轨迹”。
- 【可复用点】外部依赖失败时，至少要保留“原始 message/status/code”到日志和前端；产品可以包装成友好文案，但不能把原因全抹掉。

### I-10 用户环境 Docker daemon 未启动

- 【现象】用户在自己 PowerShell 跑 `docker compose up --build` 报 `npipe:////./pipe/dockerDesktopLinuxEngine` 找不到，无法启动。
- 【原因】不是项目问题，是 Docker Desktop 没有运行/引擎没起来。
- 【处理】不写代码时先区分环境问题：列出“本项目已用本开发代理启动的服务仍在运行，直接访问 localhost”；如果非要本地跑，则需先启动 Docker Desktop。
- 【可复用点】把“部署/运行层”和“业务代码层”分开排查；`Docker Desktop 未运行 -> 全部容器命令失败`，这不属于程序 bug，但面试时可以讲如何快速判别环境问题。

## J. 项目演进：“每引入一个技术栈解决了什么问题”

> 这个项目是从一个“简化工单 Demo”逐步长成“单 Agent + 多工具 + 工程化”的过程。面试时讲演进链路，比只报最终技术栈更真实。

### J-1 第一步：用 FastAPI + SQLite 把工单闭环跑起来

- 【新栈】FastAPI / SQLite。
- 【解决了什么】先解决“有没有”：快速提供工单 CRUD、客户/客服/管理员角色、基础权限、持久化文件型数据库。
- 【演进价值】不一开始上复杂框架，而是用最少代码验证业务闭环和 API 边界。

### J-2 第二步：引入 Vue3 + Vite + Element Plus 做 SPA

- 【新栈】Vue3 / Vite / Element Plus。
- 【解决了什么】Jinja2 服务端渲染太长异步 Agent 页面不友好；SPA 便于统一管理 loading、错误态、角色化菜单，也让前端可以独立构建部署。
- 【演进价值】前后端分离后，Agent 长耗时接口可以稳定做 loading/重试，后续还能接 WebSocket 推送。

### J-3 第三步：LangGraph 与单 Agent 多工具编排

- 【新栈】LangGraph + LangChain StructuredTool + DeepSeek。
- 【解决了什么】把“分类 → 检索 → 回复 → 监督转人工”从手工 if/else 升级成显式状态图，又让一个 Agent 用多个可行工具自主调用，LLM 不可用时回退底层图。
- 【演进价值】从“规则流程”升级到“能自主决策的客服流程”，这是项目的核心壁垒：不是单纯调 API，而是有可见、可控的工作流。

### J-4 第四步：官方 MCP SDK 把能力暴露成标准工具

- 【新栈】官方 MCP Python SDK。
- 【解决了什么】工单 CRUD、Knowledge、process_ticket 可以一套工具同时被 Web 前端 Agent 和外部 Agent 宿主使用 / Cursor 使用。
- 【演进价值】MCP 让“同一种业务能力”只写一遍，外部 Agent 迁移时不需要重写集成层。

### J-5 第五步：知识库/RAG 与 ChromaDB

- 【新栈】ChromaDB + 向量搜索。
- 【解决了什么】用相似工单/知识库辅助生成回复，减少闭眼猜答案；同时支持后续统一历史方案、补偿入口等。
- 【演进价值】RAG 带来“可引用、可更新、可观测”的知识库，避免把话术硬编码 prompt。

### J-6 第六步：人机协作闭环

- 【新栈】human-in-the-loop、RLHF、待人工审核队列。
- 【解决了什么】低置信度或敏感工单不直接给客户，而是进人工工作台；客服/主管可审核、改方案、收集 RLHF。
- 【演进价值】把 Agent 从“放出一条答案”升级到“生产可用的小流量客服台”。

### J-7 第七步：业务能力外接第三方物流（快递100）

- 【新栈】快递100 即时查询 API、工单结构化字段。
- 【解决了什么】客服/Agent 能查真实物流轨迹，不只是拼话术；没有单号也先从描述回退识别。
- 【演进价值】第三方业务集成让“智工单”开始处理真实业务数据，而不是只靠内部模拟数据。

### J-8 第八步：工程化与可观测

- 【新栈】Docker Compose、Redis、Prometheus、监控导出、环境变量透传。
- 【解决了什么】一键部署，日志和指标可查，第三方 key 按环境注入，避免开发/上线不一致。
- 【演进价值】从“单机本地能跑”升级到“同学多人/测试环境可启动”，这是项目更接近可实习的状态。

### J-9 给面试的一句话总结

- 这个项目是一次次从“快验证闭环”到“Agent 可控、RAG 可查、人工可兜底、第三方可接入”的演进；每引入一个技术都为了把“能跑”变成“能交付”。

## K. 测试与业务指标：如何让指标达标

> 面试时一定会被问“你怎么验证模型效果/业务指标？” 不编数字，用项目已有的评估数据和 API 验证方法回答。

### K-1 后端单元测试与 E2E 验证

- 【测试方法】后端 18 条 smoke test，每次改代码后跑 `docker run ... python -m unittest discover -s tests -v`。
- 【覆盖范围】工单 CRUD、角色权限、会话隔离、通知、多轮对话、指派权限、客服代录等。
- 【E2E 做法】用 PowerShell 脚本模拟客户登录 → 创建工单 → 客服领取 → 多轮对话 → 验证消息角色，真实 API 调用，不依赖 UI。
- 【面试话术】“我每次改动后跑一遍全量回归；对关键路径（比如客服对话、物流查询）我会写一次性的 E2E 脚本，确保 roles 正确、接口返回符合预期。”

### K-2 现有评估数据与指标（项目内 eval 目录）

- 【检索 Recall@K】`recall_at_k=1.0`（13 条 queries，top 3）。 来源：`backend/eval/datasets/retrieval.json` + `backend/eval/run_eval.py`。
- 【分类准确率】`accuracy=0.9375`，来源：`backend/eval/datasets/classification.json`。
- 【自动解决率】`e2e_auto_solve_rate=1.0`，来源：`backend/eval/results.json`。
- 【RLHF 偏好】`adoption_rate=0.9333`（15 条 preference pairs，14 good / 1 bad，bad 已记录修正），来源：`backend/eval/rlhf_metrics.json`。
- 【面试话术】“我有一份小评估集，验证检索召回率、分类准确率，以及 RLHF 偏好数据；这些数字不是我编的，而是项目里跑过 eval 脚本产出的。”

### K-3 物流功能上线后的“指标提升”（定性 + 定量）

- 【方法】不是靠“主观感觉”，而是用 API 验证 + 日志对比判断是否提升。
- 【定性提升】物流工单在分类白名单加“物流”之前，统计上会被归入“其他”；加之后新创建的物流工单均返回 `category=物流`，RAG 搜索也按物流知识库命中，回复从“已记录”变成“查真实轨迹 + 超时/派送/签收处理”。
- 【定量提升】快递100 集成后，用工单结构化字段 `shipper_code/tracking_no` 查询真实轨迹，前端直接展示 `Traces` 列表；不再像之前依赖描述自然语言识别，减少“无法识别”错误。
- 【面试话术】“我把物流类工单从‘其他’分类里拉出来，配合工单字段和快递100 真实查询，让回复从通用模板变成可追踪轨迹；这个提升通过 API 对比和分类统计就能体现。”

### K-4 可观测与监控

- 【Prometheus 指标】`/api/metrics` 暴露 `ecommerce_agent_processed_total`、`escalation_rate`、`redis_connected` 等。
- 【Grafana 仪表盘】预置“电商客服工单系统”面板，展示工单总数、已解决数、LLM 延迟、服务可用性。
- 【Alertmanager】后端不可用、Redis 不可用、LLM 平均延迟过高三条告警。
- 【面试话术】“我不仅验证功能，也把业务指标暴露到 Prometheus，用 Grafana 看板监控；如果某天自动解决率突然下降，我会第一时间从日志和指标里定位原因。”

### K-5 如何让指标“持续达标”而不是“一次性跑通”

- 【回归测试】每次改代码跑 `unittest`，确保不破坏已有功能。
- 【评估集覆盖】分类/检索/RLHF 三条评估集互相独立，新增功能后补评估用例。
- 【API 契约】所有新增接口跑一次真实请求，确认返回结构和关键字段。
- 【面试话术】"指标不是跑一次就够；我会把评估脚本和单元测试放进 CI（如果有），确保每次改动都有数据可追溯。"

### K-6 上线准入指标

- 【Top-3 检索召回率】目标 >= 85%
  - 口径：知识库检索结果包含正确答案的比例；当前项目 eval 里 recall_at_k=1.0（13 条 queries），高于目标。
  - 面试怎么说："我不要主观觉得搜得好，而是定义 Recall@3 = 正确答案是否出现在返回的 top 3 文档里，用 golden query-doc 评估集持续测。"
- 【幻觉率】目标 <= 5%
  - 口径：AI 回复中包含事实性错误的比例；建议抽样 50-100 条真实/构造工单，结合人工或 LLM judge 判定"信息是否与检索内容/工具结果一致"。
  - 面试怎么说："幻觉不只看 prompt，还有三道防线：检索证据 -> 工具结果 -> 低置信度转人工；再用量化幻觉率回测趋势。"
- 【格式正确率】目标 >= 95%
  - 口径：AI 回复格式符合模板规范的比例（如问候语、状态、下一步动作、结尾）。
  - 实现方式：加分 format validator，对回复做规则校验（是否有必填分段、是否纯 Markdown 滥用、是否包含联系方式/工单号等）。
  - 面试怎么说："光有内容对还不够，客服回复要有稳定模板；我会用正则/structure validator 自动抽检，不合格则重试或转人工。"
